interface:
  display_name: "SLT Hybrid v0.3 · Cost Aware"
  short_description: "Terra leads; task guards constrain workers; Sol handles unresolved decisions and risky review."
  brand_color: "#7C3AED"
  default_prompt: "Use $sol-luna-team v0.3.0 with a Terra High parent. Classify decision and review risk separately, use the deterministic routing/task guards, and reserve Sol High for required gates."
policy:
  allow_implicit_invocation: false
