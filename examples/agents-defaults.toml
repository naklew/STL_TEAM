# Example only. Prefer installing the project-scoped bundle with:
#   python scripts/slt_setup.py install /path/to/project
#
# Do not set default_subagent_model. Named agents keep Luna/Terra/Sol routing explicit.

[agents]
enabled = true
max_concurrent_threads_per_session = 2
