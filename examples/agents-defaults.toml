# Example only. Prefer the project-scoped template at:
# templates/project/.codex/config.toml
#
# Do not set default_subagent_model to Luna globally. Named agents should make
# Luna/Terra/Sol routing explicit.

[agents]
enabled = true
max_concurrent_threads_per_session = 2
